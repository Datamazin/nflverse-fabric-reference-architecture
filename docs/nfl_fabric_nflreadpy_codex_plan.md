# NFL Fabric NFLVerse Data Acquisition Implementation Plan

The original `nflreadpy` implementation plan has been superseded by the root
[README.md](../README.md), which now documents the current solution and the
steps required to rebuild it end to end. In this repo, NFLVerse is the data
source, while `nflreadpy` is the Python library used to acquire NFLVerse data.
